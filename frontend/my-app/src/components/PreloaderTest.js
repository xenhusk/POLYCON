import React from 'react';
import PreLoader from './PreLoader';

const PreloaderTest = () => (
  <div className="min-h-screen" style={{ backgroundColor: '#0065A8' }}>
    <div className="flex justify-center items-center min-h-screen">
      <PreLoader />
    </div>
  </div>
);

export default PreloaderTest;
